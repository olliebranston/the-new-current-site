name: Update carbon data

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/fetch_live_carbon_data.py"
      - "scripts/validate_site.py"
      - "requirements.txt"
      - ".github/workflows/update-carbon-data.yml"

jobs:
  update-data:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run live carbon data script
        run: python scripts/fetch_live_carbon_data.py

      - name: Validate site data
        run: python scripts/validate_site.py

      - name: Commit updated data files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || git commit -m "Update live carbon data"
          git push
